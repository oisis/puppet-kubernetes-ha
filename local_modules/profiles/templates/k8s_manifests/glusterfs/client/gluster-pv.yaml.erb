apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "gluster-vol01"
spec:
  capacity:
    storage: "2Gi"
  accessModes:
    - ReadWriteMany
  glusterfs:
    endpoints: "glusterfs-cluster"
    path: "/mnt/brick1/vol01"
    readOnly: false
  persistentVolumeReclaimPolicy: "Recycle"
